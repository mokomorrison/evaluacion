import { FiltroPropietarioPipe } from './filtro-propietario.pipe';

describe('FiltroPropietarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPropietarioPipe();
    expect(pipe).toBeTruthy();
  });
});
